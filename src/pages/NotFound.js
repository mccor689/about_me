import { Fragment } from "react";

const NotFound = () => {
    return (
        <Fragment>
            <p>This page does not exist</p>
            <p>Use the navigation at the top to find the contents of this website.</p>
        </Fragment>
    );
};

export default NotFound;